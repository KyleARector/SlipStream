# Build output
build/
sdkconfig.old

# IDF component manager
managed_components/
dependencies.lock

# VS Code / CLion IDE cruft (only if not intentionally tracked)
.vscode/
.idea/
compile_commands.json

# macOS
.DS_Store

# Host unit test build artifacts
test/host/build/
*.o
*.gcno
*.gcda

# Python virtualenvs (e.g. for tools/test_printer.py)
test_env/
.venv/
venv/

# API credentials (server URL, API key) -- compiled in at build time.
# Copy main/secrets.h.example to main/secrets.h and fill in real values.
main/secrets.h
