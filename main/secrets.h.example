#pragma once

/* Copy this file to secrets.h (gitignored, never commit real values) and
 * fill in the fields below. secrets.h is compiled in at build time -- see
 * the Phase 2 spec's "Confirmed Architecture Decisions" for why this is an
 * accepted exception to keeping credentials out of source. */

#define API_SERVER_URL "https://example.invalid"
#define API_KEY "replace-with-real-api-key"
